# Validation — project-scaffold (Phase 0)

1. `source .venv/bin/activate && pip install -r requirements.txt` completes without errors.
2. `streamlit run app.py` starts without exceptions (check terminal output).
3. Sidebar shows Home + 6 placeholder pages in the correct order.
4. Click through each page — loads without error, shows title + "Coming in Phase N" placeholder text.
5. Stop the server cleanly (Ctrl+C) — no leftover errors in terminal.
